open Css;

let fontSizeBase = px(16);
let fontSizeSM = px(14); /* 14px */
let fontSizeMD = px(18); /* 18px */

let fontFamilySans = "Helvetica Neue, Arial, sans-serif";

module Colors = {
  let n800 = hex("333333");
  let n200 = rgba(0, 0, 0, 0.2);
  let n100 = rgba(0, 0, 0, 0.08);
};